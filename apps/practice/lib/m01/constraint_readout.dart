import 'package:flutter/material.dart';

class ConstraintReadout extends StatelessWidget {
  const ConstraintReadout({super.key});

  @override
  Widget build(BuildContext context) {
    throw UnimplementedError();
  }
}
